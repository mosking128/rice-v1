# PicoC on STM32H750

[English](README.md) | [中文](README_CN.md)

本项目将 PicoC 移植到 STM32H750，在 `USART1` 上提供交互式 REPL、整文件上传执行和 Windows 上位机工具，目标是用尽量小的代价跑通 MCU 侧 C 脚本解释工作流，并沉淀一套可复用的 PicoC 嵌入式移植思路。

## 功能特性

- 基于 `USART1` 的 PicoC 交互式 REPL
- 基于 `:load / :end / :abort` 的整文件上传执行
- 支持多行源码输入和整文件运行
- 支持基础交互式调试：断点、单步、表达式求值
- 提供 STM32H750 的 Keil MDK 工程
- 提供 Windows 上位机工具，用于串口控制、文件上传、批量测试和调试

## 调试能力

当前上位机已经支持一套可用的调试流程：

- 设置断点
- 清除断点
- 单步执行
- 继续执行
- 停在断点后做表达式求值

### 调试工具栏

上位机窗口底部常驻调试工具栏：

| 区域 | 控件 | 说明 |
|------|------|------|
| 断点 | `行号` + `设断点` / `清断点` | 在上传文件执行前设置或清除断点 |
| 执行 | `继续` / `单步` | 继续运行或按语句单步 |
| 表达式 | `表达式` + `求值` | 在当前断点上下文里求值 |

### 调试流程

1. 连接板卡，等待出现 `picoc> `。
2. 输入断点行号，例如 `5`，点击“设断点”。
3. 上传并执行 `.c` 文件。
4. 命中断点后，上位机会显示 `已中断: 第5行`。
5. 点击“单步”可继续逐语句执行，或输入 `x + 1` 这类表达式后点击“求值”。
6. 点击“继续”可继续运行到下一个断点或程序结束。
7. 每次文件执行结束后，当前断点会自动清空。

### 当前边界

- 调试当前主要针对上传执行的 `serial_load` 源码。
- 断点行号按上传文件内容计算，不针对 REPL 历史输入。
- `:eval` 当前内部会包装成 `printf("%d\\n", (...))`，因此更适合整数类表达式。
- 断点会复制到隔离的文件执行实例中，运行结束后自动清理。

## 仓库结构

- [Core](Core)：STM32 应用代码
- [picoc](picoc)：PicoC 上游源码和 STM32 平台适配
- [MDK-ARM](MDK-ARM)：Keil 工程文件
- [tools/picoc_host/src](tools/picoc_host/src)：上位机源码
- [tools/picoc_host/src/README.md](tools/picoc_host/src/README.md)：上位机使用说明
- [docs/移植流程.md](docs/%E7%A7%BB%E6%A4%8D%E6%B5%81%E7%A8%8B.md)：移植流程总结
- [docs/调试使用说明.md](docs/%E8%B0%83%E8%AF%95%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md)：断点与单步说明

## 当前范围

- 脚本输入来源为串口
- 目标端不依赖文件系统加载源码
- 优先保证最小标准库可用
- 重点是稳定跑通嵌入式交互和文件执行流程

## 固件快速开始

1. 用 Keil 打开 [MDK-ARM/UART_DMA_H750.uvprojx](MDK-ARM/UART_DMA_H750.uvprojx)。
2. 编译并烧录到 STM32H750。
3. 打开 `USART1`，波特率 `115200`。
4. 上电后等待 `picoc> ` 提示符。

## 上位机快速开始

源码方式运行：

```powershell
cd tools/picoc_host
run_host.bat
```

打包方式运行：

- 运行 `build_exe.bat`
- 将 `tools/picoc_host/release/PicoCHost` 整个目录发给其他用户

## 典型使用流程

### 文件上传执行

1. 连接板卡并打开上位机。
2. 等待 `picoc> `。
3. 向文件列表中添加 `.c` 文件，选中后点击“执行选中”。
4. 上位机会自动上传源码，目标端执行后在控制台输出结果。

### 断点调试

1. 连接设备，输入断点行号并点击“设断点”。
2. 上传执行文件。
3. 程序停下后，用“单步”“求值”“继续”检查运行状态。
4. 该轮执行结束后，断点自动清空。

## 说明

- 当前目标端上传缓冲区使用 RAM。
- 仓库中已经把上位机源码与打包产物分开。
- Git 默认忽略构建产物和本地打包缓存。

## 许可证

MIT，见 [LICENSE](LICENSE)。
